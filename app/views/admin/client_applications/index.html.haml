%div
  .panel.full.section.hfeed
    .header
      %h2 API Client Applications
      %ul.actions
        %li= link_to "Register new client application", :action => :new
        %li.input= render :partial => '/admin/search', :locals => { :model => ClientApplication, :name => lambda { |comment| "ClientApplication #{comment.id}" } }
    .body
      #results
        = render :partial => 'index'
    .footer


-# OAuth plugin provides this, but I don't think we want it:

-# %h2 Tokens issued
-# 
-# - unless @tokens.empty?
-#   %table
-#     %tr
-#       %th Application
-#       %th Issued
-#       %th &nbsp;
-#     - @tokens.each do |token|
-#       - content_tag_for :tr, token do
-#         %td= link_to token.client_application.name, token.client_application.url
-#         %td= token.authorized_at
-#         %td
-#           - form_tag :controller => 'oauth', :action => 'revoke' do
-#             = hidden_field_tag 'token', token.token
-#             = submit_tag "Revoke!"
-# 