-# ------- STYLE NEEDED ------ (popup is too big for map; maybe map itself should be larger?)
.map-popup
  = render :partial => 'ideas/idea', :locals => { :idea => idea }
  - if logged_in? && current_user.admin
    = link_to 'Edit idea', edit_admin_idea_path(idea)
