Kære <%= @user.name %><br/><br/>
Dit password til <%= link_to "Ideportalen", "#{PRODUCTION_HOST}" %>  er blevet ændret. Hvis det er en fejl, så
send en mail til


Your <%= SHORT_SITE_NAME %> password has changed.

If you did not initiate this change, please notify us immediately:

  <%= @url %>